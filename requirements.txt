# ======================================================================
# DEPENDENCIAS PRINCIPAIS DA APLICACAO
# Bibliotecas que o app.py usa para funcionar.
# ======================================================================
streamlit          # Framework web para a interface
groq               # SDK da API Groq para acesso a IA
python-dotenv      # Carregar variaveis de ambiente (.env)
pillow             # Manipulacao de imagem (favicon)

# ======================================================================
# DEPENDENCIAS DE ALVO PARA SCRIPTS GERADOS PELA IA
# Opcionais para o funcionamento do assistente, mas recomendadas para
# executar scripts sugeridos pela IA.
# ======================================================================

# --- Automacao de Rede e Acesso Remoto ---
paramiko           # Automacao de SSH
netmiko            # Automacao CLI de equipamentos de rede (Cisco etc.)

# --- Cloud ---
boto3              # SDK da AWS para Python
google-cloud       # SDK do Google Cloud
azure-mgmt-compute # SDK do Azure para gerenciamento de VMs

# --- Analise de Dados e Sistema ---
pandas             # Analise e manipulacao de dados (ex: logs em CSV)
psutil             # Monitoramento de sistema (CPU, RAM, disco)
requests           # Chamadas HTTP/API

# --- Formatacao e Utilidades ---
pyyaml             # Manipulacao de arquivos YAML
colorama           # Cores no terminal
tabulate           # Tabelas formatadas no terminal
